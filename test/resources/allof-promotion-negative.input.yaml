openapi: 3.0.3
paths:
  /x:
    get:
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Parent1"
components:
  schemas:
    Parent1:
      type: object
    Child1:
      allOf:
        - $ref: "#/components/schemas/Parent1"
        - type: object
          properties:
            foo:
              type: string
    Parent2:
      type: object
    Child2:
      allOf:
        - $ref: "#/components/schemas/Parent2"
        - type: object
          properties:
            foo:
              type: string

    Unrelated:
      type: string
