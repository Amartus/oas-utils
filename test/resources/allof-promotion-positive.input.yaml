openapi: 3.0.3
paths:
  /x:
    get:
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Parent'
components:
  schemas:
    Parent:
      type: object
    Child:
      allOf:
        - $ref: '#/components/schemas/Parent'
        - type: object
          properties:
            foo:
              type: string
    Unrelated:
      type: string
